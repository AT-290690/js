
  # Code editor with Monaco

  This is a code bundle for Code editor with Monaco. The original project is available at https://www.figma.com/design/ly9DKUHN57bzFdVVNOwH93/Code-editor-with-Monaco.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  